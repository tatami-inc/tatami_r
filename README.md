# Convert R objects to `tatami` objects

This implements an **Rcpp**-based header-only library for parsing R objects as `tatami::Matrix` instances,
for use in [**tatami**](https://github.com/LTLA/tatami)-compatible C++ code such as [**libscran**](https://github.com/LTLA/libscran).
